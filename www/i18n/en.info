; Common
"Operation impossible because data have changed since recovery" "Operation impossible because data have changed since recovery"
; URL
"There are missing parameters" "There are missing parameters"
; Password
"Passwords don't match" "Passwords don't match"
"Password security is too low" "Password security is too low. It must respect: size >= %s, uppercase letter(s) >= %s, digit(s) >= %s"
; Login
"Bad password" "Bad password"
; Token
"Token argument not found" "Token argument no found"
"Invalid token" "Invalid token"
; User
"User already exists" "User already exists"
"User account is blocked" "User account is blocked"
; Account
"An account with same name already exist" "An account with same name already exist"
"Account doesn't exist" "Account doesn't exist"
